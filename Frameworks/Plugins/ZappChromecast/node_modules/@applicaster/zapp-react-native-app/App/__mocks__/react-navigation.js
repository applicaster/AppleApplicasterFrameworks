import * as React from "react";
import { View } from "react-native";

const navigationProp = {
  push: jest.fn(),
  replace: jest.fn(),
  state: {
    params: {},
  },
};

module.exports = {
  createStackNavigator: jest.fn(() => {
    const DummyComponent = props => (
      <View navigation={navigationProp} {...props} />
    );
    return DummyComponent;
  }),
};
