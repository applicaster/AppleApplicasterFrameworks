export function functionCallHelper(fn) {
  if (fn && typeof fn === "function") {
    return fn;
  }
  /* eslint-disable no-console */
  console.warn(
    `function ${(fn || {}).name || ""} is not defined in the components props !`
  );
  return () => ({});
}
