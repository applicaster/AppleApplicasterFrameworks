const shelljs = jest.genMockFromModule("fs");

function exec(command) {
  if (command.includes("FAILING_COMMAND")) {
    return { code: 1, stdErr: "Error" };
  }
  return { code: 0 };
}

function mv(file, destination) {
  // moving file
}

function rm(file) {
  // removing file
}

shelljs.exec = jest.fn(exec);
shelljs.mv = jest.fn(mv);
shelljs.rm = jest.fn(rm);

module.exports = shelljs;
