import { connectToStore } from "@applicaster/zapp-react-native-redux";
import * as R from "ramda";
import { withNavigator } from "@applicaster/zapp-react-native-ui-components/Decorators/Navigator";
import { withFocusManagerProvider } from "@applicaster/zapp-react-native-utils/focusManager";
import { LayoutComponent } from "./Layout";

export const Layout = R.compose(
  withNavigator,
  withFocusManagerProvider,
  connectToStore(R.pick(["plugins", "rivers", "appState"]))
)(LayoutComponent);
