// @flow
import * as React from "react";
import { View } from "react-native";

type Props = {
  item: {
    id: string,
    title: string,
    screen_type: ?string,
  },
  CellRenderer: React.componentType<{ item: Object, state: string }>,
  onLayout: ?(any) => void,
  focused: boolean,
  style: any,
};

export function Cell({ focused, item, CellRenderer, onLayout, style }: Props) {
  const cellState = focused ? "selected" : "default";
  return (
    <View style={style} onLayout={onLayout}>
      <CellRenderer item={item} state={focused ? "focused" : cellState} />
    </View>
  );
}
