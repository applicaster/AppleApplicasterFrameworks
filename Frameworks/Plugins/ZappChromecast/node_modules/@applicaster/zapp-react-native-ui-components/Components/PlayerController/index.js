import * as R from "ramda";

import { connectToStore } from "@applicaster/zapp-react-native-redux";

import { withNavigator } from "@applicaster/zapp-react-native-ui-components/Decorators/Navigator/";

import { PlayerController as PlayerControllerComponent } from "./PlayerController";
import { DisplayStateContext, PlayerContentContext } from "../../Contexts";

export const PlayerController = R.compose(
  connectToStore(R.pick(["plugins"])),
  withNavigator,
  DisplayStateContext.withConsumer,
  PlayerContentContext.withConsumer
)(PlayerControllerComponent);
