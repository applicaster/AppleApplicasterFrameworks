import rivers from "./<%= configuration.platform %>/rivers.json";
import styles from "./<%= configuration.platform %>/styles.json";
import pluginConfigurations from "./<%= configuration.platform %>/plugin_configurations.json";
import remoteConfigurations from "./<%= configuration.platform %>/remote_configurations.json";
import runtimeConfigurationUrls from "./<%= configuration.platform %>/runtime_configuration_urls.json";
<% if (configuration.buildParams.cell_styles_url) { %>import cellStyles from "./<%= configuration.platform %>/cell_styles.json";<% } %>

export {
  rivers,
  styles,
  pluginConfigurations,
  remoteConfigurations,
  runtimeConfigurationUrls,
  <% if (configuration.buildParams.cell_styles_url) { %>cellStyles,<% } %>
};
