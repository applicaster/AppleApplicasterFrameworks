import { Platform } from "react-native";

export const isAppleTV = () => {
  return !!(Platform.OS === "ios" && Platform.isTV);
};

export const isAppleMobile = () => {
  return !!(Platform.OS === "ios" && Platform.isTV === false);
};
