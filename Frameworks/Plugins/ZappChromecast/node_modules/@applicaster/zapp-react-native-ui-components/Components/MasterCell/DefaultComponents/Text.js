// @flow
import * as React from "react";
import { Text } from "react-native";

type Props = {
  style: any,
  label: string,
  [string]: any,
};

const _Text = ({ style, label, ...otherProps }: Props) => (
  <Text style={style} {...otherProps}>
    {label}
  </Text>
);

export default _Text;
