import React from "react";

function FocusableChildComponent(props) {
  return props.renderItem({ ...props });
}

function areEqual(prev, current) {
  return prev.focused === current.focused;
}

export const FocusableChild = React.memo(FocusableChildComponent, areEqual);
