import * as React from "react";

export function createRouterDecorator(Router) {
  return function decorateWithRouter(Component) {
    return function WrappedWithRouter(props) {
      return (
        <Router>
          <Component {...props} />
        </Router>
      );
    };
  };
}
