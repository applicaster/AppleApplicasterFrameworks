import * as R from "ramda";

import { connectToStore } from "@applicaster/zapp-react-native-redux";
// eslint-disable-next-line
import { ScreenScrollingContext } from "@applicaster/zapp-react-native-tvos-ui-components/Contexts";
import { ScreenPickerComponent } from "./ScreenPickerComponent";

export const ScreenPicker = R.compose(
  connectToStore(R.pick(["rivers"])),
  ScreenScrollingContext.withConsumer
)(ScreenPickerComponent);
