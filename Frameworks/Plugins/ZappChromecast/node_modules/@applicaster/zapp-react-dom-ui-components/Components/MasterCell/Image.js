// @flow
import * as React from "react";

type Props = {
  style: {},
  uri: string,
  [string]: any,
};

export function Image({ style, uri, ...otherProps }: Props) {
  return <img src={uri} style={style} {...otherProps} />;
}
