import * as R from "ramda";

import { connectToStore } from "@applicaster/zapp-react-native-redux";

import { BufferAnimationComponent } from "./BufferAnimation";

export const BufferAnimation = R.compose(
  connectToStore(R.pick(["styles"]))
)(BufferAnimationComponent);
