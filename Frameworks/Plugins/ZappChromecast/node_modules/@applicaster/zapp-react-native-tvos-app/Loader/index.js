import { loadLocalizations } from "./loadLocalizations";

export function appLoader(dispatch, getState) {
  const { localizations } = getState();
  return new Promise((resolve, reject) => {
    loadLocalizations(localizations, dispatch)
      .then(resolve)
      .catch(reject);
  });
}
