import * as R from "ramda";

import { ZappUIComponent } from "@applicaster/zapp-react-native-ui-components/Components/ZappUIComponent";
import { connectToStore } from "@applicaster/zapp-react-native-redux";

import { ContainerContext, ScreenScrollingContext } from "../../Contexts";
import { HorizontalListComponent } from "./HorizontalListComponent.js";

export const HorizontalList = R.compose(
  connectToStore(R.pick(["remoteConfigurations"])),
  ScreenScrollingContext.withConsumer,
  ContainerContext.withConsumer,
  ZappUIComponent
)(HorizontalListComponent);
