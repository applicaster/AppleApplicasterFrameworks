const program = jest.genMockFromModule("commander");
const R = require("ramda");

/**
 * The commander mock doesn't do anything, but returns the program
 * instance, to make sure mocked methods are chainable too
 */

program.command = R.always(program);
program.option = R.always(program);
program.action = R.always(program);

module.exports = program;
