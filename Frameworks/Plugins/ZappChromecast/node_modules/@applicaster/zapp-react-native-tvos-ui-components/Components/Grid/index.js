import * as R from "ramda";

import { connectToStore } from "@applicaster/zapp-react-native-redux";

import { ScreenScrollingContext } from "../../Contexts";
import { ZappUIComponent } from "@applicaster/zapp-react-native-ui-components/Components/ZappUIComponent";
import { GridComponent } from "./GridComponent.js";

export const Grid = R.compose(
  connectToStore(R.pick(["remoteConfigurations", "styles"])),
  ScreenScrollingContext.withConsumer,
  ZappUIComponent
)(GridComponent);
