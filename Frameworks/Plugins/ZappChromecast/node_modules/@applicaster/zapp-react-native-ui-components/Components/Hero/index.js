import { connectToStore } from "@applicaster/zapp-react-native-redux";
import { HeroComponent } from "./Hero";
import * as R from "ramda";

export const Hero = connectToStore(R.pick(["styles"]))(HeroComponent);
