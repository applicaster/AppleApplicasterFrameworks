import * as R from "ramda";
import { FocusableContext } from "@applicaster/zapp-react-native-ui-components/Contexts";
import { FocusableGroupContextWrapperComponent } from "./FocusableGroupContextWrapperComponent.js";

export const FocusableGroupContextWrapper = R.compose(
  FocusableContext.withProvider,
  FocusableContext.withConsumer
)(FocusableGroupContextWrapperComponent);
