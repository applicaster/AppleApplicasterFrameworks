import * as React from "react";

export function assignDataSource(feed) {
  return function WrappedWithAssignedDataSource(Component) {
    return function WithAssignedDataSource(props) {
      return <Component {...props} zappPipesData={{ data: feed }} />;
    };
  };
}
