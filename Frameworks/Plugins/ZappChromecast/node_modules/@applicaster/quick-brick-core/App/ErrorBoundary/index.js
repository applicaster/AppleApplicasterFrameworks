import { connectToStore } from "@applicaster/zapp-react-native-redux";
import * as R from "ramda";
import { ErrorBoundaryComponent } from "./ErrorBoundary";

const reduxConnector = connectToStore(R.pick(["components", "plugins"]));

export const ErrorBoundary = R.compose(reduxConnector)(ErrorBoundaryComponent);
