import { connectToStore } from "@applicaster/zapp-react-native-redux";
import { ListComponent } from "./List";
import * as R from "ramda";

export const List = connectToStore(R.pick(["styles", "rivers"]))(ListComponent);
