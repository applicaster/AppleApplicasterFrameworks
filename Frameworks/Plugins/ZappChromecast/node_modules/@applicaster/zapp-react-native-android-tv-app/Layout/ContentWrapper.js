import React from "react";

function ContentWrapperComponent({ children, focused, parentFocus }) {
  return React.cloneElement(children, {
    focused,
    parentFocus,
  });
}

function areEqual(prevProps, nextProps) {
  return prevProps.focused === nextProps.focused;
}

export const ContentWrapper = React.memo(ContentWrapperComponent, areEqual);
