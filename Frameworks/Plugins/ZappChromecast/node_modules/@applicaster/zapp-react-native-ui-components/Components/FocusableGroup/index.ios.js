import * as R from "ramda";
import { FocusableGroupComponent } from "./FocusableGroupComponent.js";
import { FocusableContext } from "@applicaster/zapp-react-native-ui-components/Contexts";

export const FocusableGroup = R.compose(FocusableContext.withConsumer)(
  FocusableGroupComponent
);
