import * as R from "ramda";
import { connectToStore } from "@applicaster/zapp-react-native-redux";

import { ScreenSelectorComponent } from "./ScreenSelector";

export const ScreenSelector = connectToStore(R.pick(["cellStyles"]))(
  ScreenSelectorComponent
);
