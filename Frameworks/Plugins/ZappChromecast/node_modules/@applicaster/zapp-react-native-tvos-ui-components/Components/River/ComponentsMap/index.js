import { connectToStore } from "@applicaster/zapp-react-native-redux";
import * as R from "ramda";
import { ComponentsMapComponent } from "./ComponentsMapComponent";

export const ComponentsMap = R.compose(connectToStore(R.pick(["cellStyles"])))(
  ComponentsMapComponent
);
