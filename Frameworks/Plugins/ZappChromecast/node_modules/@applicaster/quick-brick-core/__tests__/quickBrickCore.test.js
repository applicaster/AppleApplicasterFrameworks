import quickBrickCore from "../index";

describe("quickBrickCore", () => {
  it("has a createZappApp function", () => {
    expect(quickBrickCore).toHaveProperty("createZappApp");
    expect(quickBrickCore.createZappApp).toBeFunction();
  });

  it("has a renderZappAppFunction", () => {
    expect(quickBrickCore).toHaveProperty("renderZappApp");
    expect(quickBrickCore.renderZappApp).toBeFunction();
  });

  it("has a createQuickBrickApp function", () => {
    expect(quickBrickCore).toHaveProperty("createQuickBrickApp");
    expect(quickBrickCore.createQuickBrickApp).toBeFunction();
  });
});
