import { connectToStore } from "@applicaster/zapp-react-native-redux";
import * as R from "ramda";

import { ScreenResolverComponent } from "./ScreenResolver";

export const ScreenResolver = connectToStore(R.pick(["components", "plugins"]))(
  ScreenResolverComponent
);
