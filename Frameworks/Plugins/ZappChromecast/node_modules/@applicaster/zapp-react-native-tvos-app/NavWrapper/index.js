import { connectToStore } from "@applicaster/zapp-react-native-redux";
import * as R from "ramda";

import { NavWrapperComponent } from "./NavWrapper";

export const NavWrapper = R.compose(
  connectToStore(R.pick(["plugins", "rivers", "pluginConfigurations"]))
)(NavWrapperComponent);
