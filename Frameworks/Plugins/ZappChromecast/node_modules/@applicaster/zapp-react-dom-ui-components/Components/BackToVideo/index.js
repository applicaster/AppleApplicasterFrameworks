import * as R from "ramda";
import { hot } from "react-hot-loader";

import { connectToStore } from "@applicaster/zapp-react-native-redux";

import {
  DisplayStateContext,
  PlayerContentContext,
  RiverOffsetContext,
} from "../../Contexts";

import { BackToVideoComponent } from "./BackToVideo";

export const BackToVideo = R.compose(
  hot(module),
  connectToStore(R.pick(["remoteConfigurations", "localizations", "styles"])),
  RiverOffsetContext.withConsumer,
  DisplayStateContext.withConsumer,
  PlayerContentContext.withConsumer
)(BackToVideoComponent);
