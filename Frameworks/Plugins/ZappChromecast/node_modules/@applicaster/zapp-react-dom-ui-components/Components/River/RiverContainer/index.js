import * as R from "ramda";

import { RiverOffsetContext } from "../../../Contexts";
import { RiverContainerComponent } from "./RiverContainer";

export const RiverContainer = R.compose(
  RiverOffsetContext.withProvider,
  RiverOffsetContext.withConsumer
)(RiverContainerComponent);
