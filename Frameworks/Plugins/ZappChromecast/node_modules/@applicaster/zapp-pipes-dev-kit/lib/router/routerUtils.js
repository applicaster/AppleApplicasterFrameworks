'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var parseRequest = exports.parseRequest = function parseRequest(request) {
  var _request$params = request.params,
      provider = _request$params.provider,
      command = _request$params.command;

  var params = request.query;
  return { provider: provider, command: command, params: params };
};

var makeThenable = function makeThenable(object) {
  object.then = function (f) {
    return f(object);
  };
  return object;
};

var isThenable = function isThenable(object) {
  return !!object.then && typeof object.then === 'function';
};

var forceAsync = exports.forceAsync = function forceAsync(object) {
  return isThenable(object) ? object : makeThenable(object);
};